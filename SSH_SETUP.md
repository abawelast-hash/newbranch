# 🔐 إعداد الاتصال الآمن بالسيرفر

## المشكلة
يطلب SSH كلمة المرور في كل مرة، وأنا (AI) لا أستطيع حفظها بين الجلسات.

## الحل: SSH Keys (مرة واحدة فقط!)

### الخطوات:

```bash
# 1. شغّل السكريبت
./setup-ssh-key.sh

# 2. عند طلب كلمة المرور، أدخل:
Goolbx512@@

# 3. انتهى! الآن لن يطلب كلمة المرور مرة أخرى
```

---

## 🚀 النشر السريع (بعد الإعداد)

```bash
# بدلاً من كل الخطوات الطويلة، فقط:
./deploy-quick.sh
```

سيقوم تلقائياً بـ:
- ✅ Commit & Push محلياً
- ✅ تحديث السيرفر
- ✅ تشغيل Migrations
- ✅ تحديث Cache

---

## 📝 ملف الباسورد المؤقت (إذا لم تعد SSH Keys)

إذا فضلت حفظ الباسورد محلياً (غير آمن لكن سريع):

```bash
# إنشاء ملف محمي
echo "Goolbx512@@" > ~/.ssh/sarh_password
chmod 600 ~/.ssh/sarh_password

# استخدامه
sshpass -f ~/.ssh/sarh_password ssh -p 65002 u307296675@194.164.74.250
```

---

## بيانات الاتصال

```
Host: 194.164.74.250
Port: 65002
User: u307296675
Pass: Goolbx512@@
Path: /home/u307296675/sarh
```

---

## ✅ التوصية

**استخدم `setup-ssh-key.sh` مرة واحدة فقط**، وبعدها لن تحتاج كلمة المرور أبداً!
