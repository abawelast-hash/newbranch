# أسئلة توضيحية لمطور النظام — SARH v3.0

**الغرض:** هذه الوثيقة تجمع الأسئلة التي تحتاج إجابات قبل إتمام أو قفل بعض قرارات التصميم.  
**آخر تحديث:** 2026-02-27  
**الحالة:** 🔴 مفتوحة — تنتظر ردود المطور / صاحب المنتج

---

## 1. نظام التعاميم (Circulars)

| # | السؤال | الخيارات | القرار الحالي |
|---|--------|----------|---------------|
| 1.1 | هل التعاميم يُفترض أن تظهر **فقط** للمجموعة المستهدفة (`target_scope`)؟ | أ) نعم، المستهدفون فقط ب) بعضها عام مقصود | مُطبَّق: فلترة `scopeForUser` في المستوى < 10 |
| 1.2 | هل هناك حالات لعرض تعميم عام (`target_scope = all`) لغير الموظفين؟ | أ) لا ب) نعم (زوار / صفحة عامة) | يُحتاج توضيح |
| 1.3 | هل نحتاج **"قراءة / إقرار"** (Read Receipt) لكل تعميم؟ | أ) نعم → `circular_acknowledgments` ب) لا | نموذج `CircularAcknowledgment` موجود لكن غير مرتبط بـ Widget |
| 1.4 | ما الحد الأقصى لعدد التعاميم المعروضة في Dashboard Widget؟ | رقم محدد | حالياً: 5 |

---

## 2. نظام Streak والحضور

| # | السؤال | الخيارات | القرار الحالي |
|---|--------|----------|---------------|
| 2.1 | أيام العطلة الرسمية (Holiday) وساعد بها أثناء الـ Streak: | أ) لا تقطع السلسلة ✅ ب) تُحتسب حضوراً ج) تقطع السلسلة | **مُطبَّق: لا تقطع** (StreakService يتجاوزها) |
| 2.2 | الإجازة الموافَق عليها (Leave approved): | أ) لا تقطع السلسلة ✅ ب) تُحتسب حضوراً ج) تُحتسب غياباً | **مُطبَّق: لا تقطع** |
| 2.3 | الإجازة المرفوضة أو `pending`: | أ) تقطع السلسلة ✅ ب) لا تقطع | **مُطبَّق: تقطع** |
| 2.4 | هل الحضور `late` يحتسب ضمن Streak؟ | أ) نعم ✅ ب) لا | **مُطبَّق: نعم** |
| 2.5 | ما الحد الأقصى لعمق البحث في Streak (أيام للخلف)؟ | 90 / 180 / 365 يوم | حالياً: 365 |
| 2.6 | هل يُعاد حساب الـ Streak تلقائياً عند تسجيل حضور؟ | أ) نعم، invalidate cache ✅ ب) cron job فقط | **مُطبَّق: invalidate عند تسجيل الحضور** |
| 2.7 | أيام نهاية الأسبوع (Weekend): الجمعة والسبت فقط؟ | أ) جمعة + سبت ✅ ب) قابل للتخصيص per-branch | **مُطبَّق: افتراضي جمعة + سبت، قابل للتخصيص عبر `Branch.weekend_days`** |

---

## 3. نظام Trap

| # | السؤال | الخيارات | القرار الحالي |
|---|--------|----------|---------------|
| 3.1 | هل نظام Trap محذوف **نهائياً** أم معلق مؤقتاً؟ | أ) محذوف نهائياً ب) معلق مؤقتاً | **مُنفَّذ: الـ routes محذوفة، الـ migrations تُقلَّص** |
| 3.2 | هل نحذف جداول `traps` و `trap_interactions` من قاعدة البيانات الإنتاجية؟ | أ) نعم ✅ ب) نبقيها احتياطياً | Migration موجود: `drop_trap_tables` |
| 3.3 | إذا أُعيد النظام مستقبلاً، هل يكون بمعمارية مختلفة؟ | — | يُحتاج قرار |
| 3.4 | هل تريد إزالة الملفات الموجودة (TrapController, TrapResource, إلخ)؟ | أ) نعم — شغّل `php artisan system:remove-trap` ب) لا | الأمر جاهز للتنفيذ |

---

## 4. الحسابات المالية

| # | السؤال | الخيارات | القرار الحالي |
|---|--------|----------|---------------|
| 4.1 | أساس حساب الخسارة اليومية (`cost`): | أ) basic salary / أيام العمل ب) total package | يُحتاج توضيح |
| 4.2 | هل Overtime يؤثر على حساب الـ Streak؟ | أ) لا ✅ ب) يُحتسب نقاطاً إضافية | حالياً: لا |
| 4.3 | كم دقيقة سماح للتأخير قبل احتساب `cost`؟ | 0 / 5 / 10 / 15 دقيقة | يُحتاج توضيح — حالياً غير محدد |
| 4.4 | هل الإجازة المدفوعة (`paid leave`) تُطرح من الخسارة المحسوبة؟ | أ) نعم ✅ ب) لا | **مُطبَّق: الإجازات الموافَق عليها مُطرحة** |
| 4.5 | عملة الحساب؟ | SAR / USD / ديناميكية | يُحتاج توضيح |

---

## 5. الأمان والصلاحيات

| # | السؤال | الخيارات | القرار الحالي |
|---|--------|----------|---------------|
| 5.1 | هل نحتاج **2FA** للمستويات 8-10؟ | أ) نعم ب) لا | غير مُطبَّق حالياً |
| 5.2 | هل نريد **Audit Log** لكل عمليات God Mode (المستوى 10)؟ | أ) نعم → GodModeAuditService ب) لا | مقترح في Batch 5 — لم يُنفَّذ بعد |
| 5.3 | هل يجب تسجيل دخول المستوى 10 من IPs محددة فقط؟ | أ) نعم (IP Whitelist) ب) لا | غير مُطبَّق |
| 5.4 | هل Gate `manage-report-formulas` يجب أن يتطلب مستوى 10 فقط؟ | أ) نعم ✅ ب) 9+ كافٍ | حالياً: 10 |
| 5.5 | هل عمليات Leaveيجب أن تُسجَّل في `audit_logs`؟ | أ) نعم ب) لا | `AuditLog` موجود لكن غير مرتبط بـ Leave |

---

## 6. نظام الإجازات (Leaves)

| # | السؤال | الخيارات | القرار الحالي |
|---|--------|----------|---------------|
| 6.1 | أنواع الإجازة المدعومة: | annual / sick / emergency / unpaid / maternity / other | مُطبَّق في الـ enum |
| 6.2 | من يوافق على الإجازة؟ | أ) المدير المباشر فقط ب) أي مستوى 6+ | مُطبَّق: كلاهما |
| 6.3 | هل رصيد الإجازات مُحدَّد للموظف؟ | أ) نعم → حقل `annual_leave_balance` في `users` ب) لا | يُحتاج توضيح |
| 6.4 | هل الإجازة المرفوضة تعود للرصيد؟ | أ) نعم ب) لا | يُحتاج توضيح |

---

## 7. الصيغ والتقارير (SafeFormulaEngine)

| # | السؤال | الخيارات | القرار الحالي |
|---|--------|----------|---------------|
| 7.1 | المتغيرات المدعومة: هل القائمة الحالية كافية؟ | `attendance, delay_rate, on_time_rate, total_points, overtime_rate, financial_loss, task_completion` | يُحتاج مراجعة |
| 7.2 | هل نعرض محرر الصيغة في Filament Resource؟ | أ) نعم ب) JSON مباشر | يُحتاج قرار |
| 7.3 | الحد الأقصى لطول الصيغة؟ | 200 / 500 / 1000 حرف | حالياً: 500 |
| 7.4 | هل نريد **preview** لنتيجة الصيغة مع قيم مثال قبل الحفظ؟ | أ) نعم ب) لا | غير مُطبَّق |

---

## 8. قواعد البيانات والتوافق

| # | السؤال | الخيارات | القرار الحالي |
|---|--------|----------|---------------|
| 8.1 | هل `leaves` table موجودة في الإنتاج؟ | أ) نعم ب) لا — تحتاج migrate | migration جاهز: `2026_02_27_000004_create_leaves_table` |
| 8.2 | هل `circular_deliveries` موجودة؟ | أ) نعم ب) لا | migration جاهز: `2026_02_27_000002` |
| 8.3 | هل تمت إضافة `weekend_days` column إلى `branches`؟ | أ) نعم ب) لا | migration جاهز: `2026_02_27_000005` |
| 8.4 | هل جداول `traps` و `trap_interactions` موجودة في الإنتاج؟ | أ) نعم → تُحذف بالـ migration ب) لا | migration جاهز للحذف |

---

## الإجراءات الفورية المطلوبة

```bash
# 1. تشغيل جميع الـ migrations الجديدة:
php artisan migrate --force

# 2. (اختياري) حذف ملفات نظام Trap:
php artisan system:remove-trap

# 3. مسح الكاش بعد الإعداد:
php artisan optimize:clear
```

---

## جدول الأولوية

| الأولوية | السؤال | الأثر |
|----------|--------|-------|
| 🔴 عاجل | 4.3 — دقائق السماح للتأخير | يؤثر على دقة الحسابات المالية |
| 🔴 عاجل | 6.3 — رصيد الإجازات | يؤثر على LeavePolicy |
| 🟡 مهم | 5.2 — GodMode Audit Log | أمان وامتثال |
| 🟡 مهم | 1.3 — إقرار التعاميم | UX وامتثال |
| 🟢 عادي | 7.4 — preview الصيغة | تجربة مطور |
| 🟢 عادي | 2.5 — عمق Streak | أداء |
