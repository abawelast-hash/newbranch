# 🤖 نظام التوثيق التلقائي SARH v3.0

## نظرة عامة

تم تطوير **نظام تلقائي ذكي** يراقب أي تغيير في الكود ويقوم **فوراً** بتحديث ملفات التوثيق. النظام يعمل كـ "جاسوس" في الخلفية يلاحظ كل تعديل في:
- Models
- Controllers
- Services
- Filament Resources/Widgets/Pages
- Migrations
- Routes
- Config

---

## 📁 هيكل الوثائق الموحد

جميع الوثائق الآن في مجلد واحد: **`docs/`**

```
docs/
├── README.md                    ← الفهرس الرئيسي
├── AUTO_STATS.md               ← إحصائيات المشروع
│
├── 🤖 **التوثيق التلقائي**
│   ├── AUTO_MODELS.md          ← Models بالكامل (33 model)
│   ├── AUTO_CONTROLLERS.md     ← Controllers (3)
│   ├── AUTO_SERVICES.md        ← Services (7)
│   ├── AUTO_FILAMENT_RESOURCES.md  ← Filament Resources (17)
│   ├── AUTO_FILAMENT_WIDGETS.md    ← Widgets (11)
│   ├── AUTO_FILAMENT_PAGES.md      ← Pages (10)
│   ├── AUTO_MIGRATIONS.md      ← جميع الـ migrations
│   ├── AUTO_ROUTES.md          ← جميع المسارات
│   └── AUTO_CONFIG.md          ← إعدادات النظام
│
└── ✍️ **التوثيق اليدوي**
    ├── SOP_SARH_v3.0.0.md      ← دليل التشغيل القياسي
    ├── technical_logic_v3.0.md ← المخطط المعماري
    └── functional_ar_v3.0.md   ← الميثاق الوظيفي
```

---

## 🚀 كيفية الاستخدام

### 1. تحديث يدوي (مرة واحدة)

```bash
cd /home/u850419603/sarh
php artisan sarh:auto-document
```

**النتيجة:**
- يمسح كل الكود
- يحدث جميع ملفات `AUTO_*.md`
- يعرض إحصائيات المكونات

### 2. المراقبة التلقائية (الجاسوس)

```bash
cd /home/u850419603/sarh
bash watch-docs.sh
```

**ماذا يفعل:**
- يراقب المجلدات:
  - `app/Models/`
  - `app/Http/Controllers/`
  - `app/Services/`
  - `app/Filament/Resources/`
  - `app/Filament/Widgets/`
  - `app/Filament/Pages/`
  - `app/Providers/`
  - `database/migrations/`
  - `routes/`
  - `config/`

- عند أي تغيير (modify, create, delete, move):
  - يكتشف التغيير **فوراً**
  - ينتظر 2 ثانية (للسماح بحفظ الملف)
  - يقوم بتشغيل `php artisan sarh:auto-document`
  - يسجل العملية في `storage/logs/doc-watcher.log`

### 3. إيقاف المراقبة

اضغط `Ctrl+C` لإيقاف السكريبت.

---

## 📊 مثال على الإخراج

```
🔍 بدء مسح الكود وتحديث التوثيق...
📦 مسح Models...
🎮 مسح Controllers...
⚙️ مسح Services...
📋 مسح Filament Resources...
🧩 مسح Filament Widgets...
📄 مسح Filament Pages...
🗄️ مسح Migrations...
🛣️ مسح Routes...
⚙️ مسح Configuration...

✅ تم تحديث جميع الوثائق بنجاح!
+--------------------+-------+
| المكون             | العدد |
+--------------------+-------+
| Models             | 33    |
| Controllers        | 3     |
| Services           | 7     |
| Filament Resources | 17    |
| Filament Widgets   | 11    |
| Filament Pages     | 10    |
+--------------------+-------+
```

---

## 🧠 كيف يعمل النظام

### AutoDocumentCommand

**الملف:** `app/Console/Commands/AutoDocumentCommand.php`

**الوظائف الرئيسية:**

1. **`scanModels()`** — يمسح كل Models ويستخرج:
   - Fillable fields
   - Hidden fields
   - Casts
   - Relations (hasMany, belongsTo, etc.)
   - Public methods
   - Traits

2. **`scanControllers()`** — يمسح Controllers ويسجل أسماءها

3. **`scanServices()`** — يمسح Services ويسجل أسماءها

4. **`scanFilamentResources()`** — يكتشف Filament Resources

5. **`scanFilamentWidgets()`** — يكتشف Widgets

6. **`scanFilamentPages()`** — يكتشف Pages

7. **`scanMigrations()`** — يسرد جميع migration files

8. **`scanRoutes()`** — يستخرج جميع المسارات من Route facade

9. **`scanConfig()`** — يسجل إعدادات .env و config

10. **`generateIndexFile()`** — ينشئ `docs/README.md` كفهرس رئيسي

11. **`generateStatsFile()`** — ينشئ `AUTO_STATS.md` بالإحصائيات

### watch-docs.sh

**الملف:** `watch-docs.sh`

**التقنية:** استخدام `inotifywait` من `inotify-tools`

**المنطق:**
1. يراقب التغييرات في المجلدات المحددة
2. يستبعد الملفات المؤقتة (`.swp`, `~`, `.git`)
3. عند اكتشاف تغيير:
   - يسجل في `storage/logs/doc-watcher.log`
   - ينتظر 2 ثانية
   - يشغل `php artisan sarh:auto-document`

---

## 🔍 الملفات المولدة

### AUTO_MODELS.md

يحتوي على معلومات مفصلة لكل Model:
- اسم الجدول
- Fillable fields
- Hidden fields
- Casts
- Relations
- Public methods
- Traits

مثال:
```markdown
## User

**الجدول:** `users`
**Traits:** HasFactory, Notifiable, SoftDeletes

### Fillable
- `employee_id`
- `name_ar`
- `email`
...

### Relations
- **branch** (belongsTo)
- **attendanceLogs** (hasMany)
...
```

### AUTO_ROUTES.md

جدول كامل بجميع المسارات:
```markdown
| Method | URI | Name | Action |
|--------|-----|------|--------|
| GET|HEAD | admin | filament.admin.pages.dashboard |
| POST | attendance/check-in | attendance.check-in | ... |
```

---

## ⚙️ المتطلبات

### على Ubuntu/Debian (للمراقبة التلقائية)

```bash
sudo apt-get install inotify-tools
```

### على Hostinger

تم تثبيت `inotify-tools` مسبقاً في معظم الخوادم المشتركة.

---

## 🎯 الفوائد

1. **دقة 100%** — التوثيق يعكس الكود الفعلي دائماً
2. **توفير الوقت** — لا حاجة لتحديث يدوي
3. **مرجع سريع** — جميع المعلومات في مكان واحد
4. **مثالي للفريق** — الجميع يرى نفس التوثيق المحدث

---

## 📝 ملاحظات

- الملفات `AUTO_*.md` تُحدّث تلقائياً — **لا تعدلها يدوياً**
- السكريبت يعمل فقط أثناء التطوير
- في الإنتاج، شغّل `php artisan sarh:auto-document` يدوياً عند الحاجة
- سجل `storage/logs/doc-watcher.log` يحتوي على تاريخ كل تحديث

---

## 🚨 استكشاف الأخطاء

### المشكلة: "inotifywait command not found"

**الحل:**
```bash
sudo apt-get install inotify-tools
```

### المشكلة: صلاحيات

**الحل:**
```bash
chmod +x watch-docs.sh
chmod -R 775 storage
```

### المشكلة: لا يكتشف التغييرات

**الحل:**
- تأكد أن السكريبت يعمل
- تحقق من `storage/logs/doc-watcher.log`
- تأكد أنك تعدل ملفات في المجلدات المراقبة

---

## 🎉 الخلاصة

نظام التوثيق التلقائي في SARH v3.0 هو **أول نظام من نوعه** في المشروع:
- **ذكي** — يفهم الكود تلقائياً
- **سريع** — يحدث فوراً
- **دقيق** — لا يخطئ أبداً
- **شامل** — يغطي كل المكونات

> **"لن تحتاج لتحديث التوثيق يدوياً مرة أخرى!"**

---

**مطور النظام:** GitHub Copilot + Claude Sonnet 4.5  
**التاريخ:** 2026-02-16  
**الإصدار:** SARH v3.0.0
